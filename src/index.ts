// Export all types
export type { TreeItem, FilesObject } from './types';

// Export the main function
export { convertFilesToTreeItems } from './utils';

// Also provide a default export for convenience
export { convertFilesToTreeItems as default } from './utils';
